"""C5 -- simulator and arms.

Cardinal values live here and nowhere else (CLAUDE.md, "Ordinal vs cardinal").
Every one of them is sampled from a range in `config/worlds.yaml` rather than
fixed, because C8's robustness sweep is many worlds and parameterisation has to
be structural rather than retrofitted.
"""
